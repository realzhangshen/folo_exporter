# Folo Exporter 重构设计

日期: 2026-01-27

## 背景

现有版本存在两个核心问题：
1. 只能抓取当前窗口可见的文章，无法获取所有未读
2. 抓取时容易触发已读状态，影响文章管理

## 目标

- 一键抓取所有未读文章（不受滚动限制）
- 不触发已读状态
- 导出为 Markdown 格式供 AI 处理
- 可接受半自动操作

## 技术方案：API 直接调用

通过 Follow 的 API 获取数据，完全绑过页面交互。

### 整体架构（简化版）

```
┌─────────────────────────────────────────┐
│           Chrome Extension              │
├─────────────────┬───────────────────────┤
│     Popup       │    Background         │
│     (UI)        │    (Service Worker)   │
├─────────────────┼───────────────────────┤
│ - 抓取按钮       │ - 调用 Follow API     │
│ - 显示进度/结果  │ - 分页获取所有未读     │
│ - 导出选项       │ - 数据格式化          │
│ - 复制/下载      │ - 生成 Markdown       │
└─────────────────┴───────────────────────┘
                        │
                        │ credentials: "include"
                        │ (自动携带 cookie)
                        ▼
              ┌───────────────────────┐
              │   https://api.folo.is │
              │   POST /entries       │
              └───────────────────────┘
```

**注意**: 不需要 Content Script！API 使用 cookie 认证，Service Worker 可以直接调用。

### 认证方式（已验证）

**无需手动提取 Token！**

Follow API 使用 Cookie-based 认证。只要用户已登录 Follow，浏览器会自动携带 session cookie。

**实现方式**:
```javascript
fetch("https://api.folo.is/entries", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  credentials: "include",  // ← 关键：携带 cookie
  body: JSON.stringify({ limit: 100, view: -1, read: false })
})
```

**用户体验**:
- 用户已登录 Follow → 直接调用 API → 全自动
- 用户未登录 → API 返回 401 → 提示"请先登录 Follow"

### API 调用与数据获取（已验证）

**API 基础 URL**: `https://api.folo.is`

**认证方式**: Cookie-based (`credentials: "include"`)，无需 Bearer Token

**获取未读文章**:
```javascript
POST /entries
Headers: { "Content-Type": "application/json" }
Body: { "limit": 100, "view": -1, "read": false }
```

**分页处理**（如果未读超过 100 条）：
- 第一次请求获取前 100 条
- 记录最后一条的 `publishedAt` 时间戳
- 后续请求添加 `publishedBefore` 参数继续获取

**实际响应结构（已验证）**：
```json
{
  "code": 0,
  "data": [{
    "read": false,
    "entries": {
      "id": "238791670480109568",
      "title": "文章标题",
      "url": "https://example.com/article",
      "publishedAt": "2026-01-27T01:53:15.699Z",
      "summary": "AI 生成的摘要（如果有）"
    },
    "feeds": {
      "title": "Hugging Face - Blog",
      "siteUrl": "https://huggingface.co/blog"
    },
    "subscriptions": {
      "category": "AI Blogs"
    }
  }]
}
```

**字段映射**:
| 导出字段 | API 路径 |
|---------|---------|
| 标题 | `data[].entries.title` |
| 链接 | `data[].entries.url` |
| 来源 | `data[].feeds.title` |
| 时间 | `data[].entries.publishedAt` |
| 摘要 | `data[].entries.summary` |
| 分类 | `data[].subscriptions.category` |

### 导出格式

```markdown
# Follow 未读文章导出
导出时间: 2026-01-27 15:30
未读数量: 103 篇

---

## AI News (32)

### 文章标题
- 来源: AI News
- 时间: 2026-01-27 10:00
- 链接: https://example.com/article1
- 摘要: 文章摘要内容...

...
```

格式选项：
- 按来源分组（默认）
- 纯列表不分组

### Popup UI

```
┌────────────────────────────────────┐
│  Folo Exporter            [状态灯] │
├────────────────────────────────────┤
│  ┌──────────────────────────────┐  │
│  │      [ 抓取未读文章 ]         │  │
│  └──────────────────────────────┘  │
│                                    │
│  状态: ● 已连接 (token 有效)       │
├────────────────────────────────────┤
│  抓取结果:                         │
│  ✓ 获取到 103 篇未读文章           │
│  · AI News: 32 篇                 │
│  · AI Blogs: 21 篇                │
│  · ...                            │
│                                    │
│  导出选项:                         │
│  ○ 按来源分组  ○ 纯列表            │
│                                    │
│  [ 复制到剪贴板 ]  [ 下载为文件 ]   │
└────────────────────────────────────┘
```

状态提示：
- 🟢 已连接 - token 有效
- 🟡 需要登录 - 请先登录 Follow
- 🔴 错误 - 显示具体错误信息

### 文件结构（简化）

```
folo_exporter/
├── manifest.json        # Chrome 扩展配置
├── popup.html           # UI 界面
├── popup.js             # UI 逻辑 + API 调用
├── popup.css            # 样式
└── README.md            # 使用说明
```

**注意**: 由于 API 使用 cookie 认证，不需要 service worker 或 content script，popup.js 可以直接调用 API。

### 技术选择

- 纯 JavaScript，不引入框架
- Manifest V3
- Fetch API

### 权限需求

```json
{
  "permissions": ["storage", "clipboardWrite"],
  "host_permissions": [
    "https://app.folo.is/*",
    "https://api.folo.is/*"
  ]
}
```

## 后续规划

- AI 接口集成（可选，后续实现）

## API 研究结果（已完成）

✅ **已验证**:
1. API 端点: `POST https://api.folo.is/entries`
2. 认证方式: Cookie-based，使用 `credentials: "include"`
3. 获取未读: 请求 body 添加 `read: false` 参数
4. 数据结构: 包含 title, url, summary, feeds.title, subscriptions.category
